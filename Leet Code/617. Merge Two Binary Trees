/**
* 617. Merge Two Binary Trees
* You are given two binary trees root1 and root2.
* Imagine that when you put one of them to cover the other, some nodes of the two trees are overlapped while the others are not. You need to merge the two trees into a new binary tree. The merge rule is 
* that if two nodes overlap, then sum node values up as the new value of the merged node. Otherwise, the NOT null node will be used as the node of the new tree.
* Return the merged tree.
* Note: The merging process must start from the root nodes of both trees.
*/


/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public void merge(TreeNode root1, TreeNode root2) {
        if(root2==null) return;
        if(root1!=null && root2!=null) {
            root1.val = root1.val+root2.val;
            
        }
        boolean leftSub = false;
        boolean rightSub = false;
        if(root1.left == null && root2.left!=null) {
            root1.left = root2.left;
            leftSub = true;
        }
        if(root1.right == null && root2.right!=null) {
            root1.right = root2.right;
            rightSub = true;
        }
        
        if(root1.left!=null && !leftSub){
            merge(root1.left,root2.left);
        }
        if(root1.right!=null && !rightSub) {
            merge(root1.right,root2.right);   
        }
    }
    public TreeNode mergeTrees(TreeNode root1, TreeNode root2) {
        if(root2 == null)
            return root1;
        if(root1==null)
            return root2;
        TreeNode temp = root1;
        merge( root1,  root2);
        return temp;
    }
}